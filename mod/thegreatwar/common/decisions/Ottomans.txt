Situation_of_debt = {

	Small_debt_payment = {

		icon = hol_attract_foreign_investors

        priority = 3

		allowed = {
			original_tag = TUR
		}

		available = {
			NOT = {
				has_country_flag = Paying_debt
			}
		}
		
		days_remove = 30

		cost = 50

		fire_only_once = no

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 10
				is_historical_focus_on = yes
			}
			modifier = {
				factor = 5
				date > 1911.01.01
			}
		}

		visible = {
			OR = {
				has_idea = Ottoman_debt_administration
				has_idea = Ottoman_debt_administration_1
				has_idea = Ottoman_debt_administration_2
			}
			NOT = {
				check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 0 compare = less_than }
			}
		}

		complete_effect = {
			add_ideas = Small_debt
			set_country_flag = Paying_debt
		}

		remove_effect = {
		    custom_effect_tooltip = Debt_10
			remove_ideas = Small_debt
			clr_country_flag = Paying_debt
			subtract_from_variable = { var = GRE_debt_to_the_ifc_britain_var value = 10 }
			if = {
			    limit = {
				    check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 80 compare = less_than }
				}
				swap_ideas = {
				    remove_idea = Ottoman_debt_administration
			        add_idea =  Ottoman_debt_administration_1
			    }
				else_if = {
				    limit = {
				        check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 45 compare = less_than }
				    }
					swap_ideas = {
				        remove_idea = Ottoman_debt_administration_1
			            add_idea =  Ottoman_debt_administration_2
			        }
				}
				else = {
				}
			}
		}
	}
	Large_debt_payment = {

		icon = gre_investment_decisions

		priority = 2

		allowed = {
			original_tag = TUR
		}

		available = {
			NOT = {
				has_country_flag = Paying_debt
			}
			has_completed_focus = TUR_ww1_infrastructure_effort_5
		}

		days_remove = 60

		cost = 100

		fire_only_once = no

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 5
				is_historical_focus_on = yes
			}
			modifier = {
				factor = 0
				date > 1914.06.01
			}
		}

		visible = {
			OR = {
				has_idea = Ottoman_debt_administration
				has_idea = Ottoman_debt_administration_1
				has_idea = Ottoman_debt_administration_2
			}
			NOT = {
				check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 0 compare = equals }
			}
			has_completed_focus = TUR_ww1_infrastructure_effort_5
		}

		complete_effect = {
			add_ideas = Large_debt
			set_country_flag = Paying_debt
		}

		remove_effect = {
		    custom_effect_tooltip = Debt_20
			remove_ideas = Large_debt
			clr_country_flag = Paying_debt
			subtract_from_variable = { var = GRE_debt_to_the_ifc_britain_var value = 20 }
			if = {
			    limit = {
				    check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 80 compare = less_than }
				}
				swap_ideas = {
				    remove_idea = Ottoman_debt_administration
			        add_idea =  Ottoman_debt_administration_1
			    }
				else_if = {
				    limit = {
				        check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 45 compare = less_than }
				    }
					swap_ideas = {
				        remove_idea = Ottoman_debt_administration_1
			            add_idea =  Ottoman_debt_administration_2
			        }
				}
				else = {
				}
			}
		}
	}
	Ask_germany_for_debt_deleting = {

		icon = hol_attract_foreign_investors

        priority = 	4	

		allowed = {
			original_tag = TUR
		}

		available = {
			NOT = {
				has_country_flag = Paying_debt
			}
			is_in_faction_with = GER
		}
		
		days_remove = 60

		cost = 100
		
		targets = { GER }

		fire_only_once = yes

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 10
				is_historical_focus_on = yes
			}
			modifier = {
				factor = 100
				date > 1915.01.01
			}
		}

		visible = {
		    is_in_faction_with = GER
			OR = {
				has_idea = Ottoman_debt_administration
				has_idea = Ottoman_debt_administration_1
				has_idea = Ottoman_debt_administration_2
			}
			NOT = {
				check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 0 compare = equals }
			}
		}

		remove_effect = {
		    FROM = { add_political_power = 100 }
		    custom_effect_tooltip = Debt_40
			subtract_from_variable = { var = GRE_debt_to_the_ifc_britain_var value = 40 }
			if = {
			    limit = {
				    check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 80 compare = less_than }
				}
				swap_ideas = {
				    remove_idea = Ottoman_debt_administration
			        add_idea =  Ottoman_debt_administration_1
			    }
				else_if = {
				    limit = {
				        check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 45 compare = less_than }
				    }
					swap_ideas = {
				        remove_idea = Ottoman_debt_administration_1
			            add_idea =  Ottoman_debt_administration_2
			        }
				}
				else = {
				}
			}
		}
	}
	Full_payment_of_the_debt = {

		icon = generic_break_treaty

		priority = 1

		allowed = {
			original_tag = TUR
		}

		available = {
			NOT = {
				has_country_flag = Paying_debt
			}
			check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 0 compare = less_than }
		}

		cost = 200

		fire_only_once = yes

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				is_historical_focus_on = yes
			}
			modifier = {
				factor = 100
				has_war_with = ENG
			    has_war_with = FRA
			}
			modifier = {
				factor = 30

			}
		}

		visible = {
			OR = {
				has_idea = Ottoman_debt_administration
				has_idea = Ottoman_debt_administration_1
				has_idea = Ottoman_debt_administration_2
			}
		}

		complete_effect = {
		    custom_effect_tooltip = Debt_must_paid
			if = {
			    limit = {
				    has_idea = Ottoman_debt_administration
				}
				remove_ideas = Ottoman_debt_administration
				else_if = {
				    remove_ideas = Ottoman_debt_administration_1
				}
				else_if = {
				    remove_ideas = Ottoman_debt_administration_2
				}
			}
		}
	}	
    Illegaly_default_on_the_debt = {

		icon = generic_break_treaty
		
		priority = 1

		allowed = { 
			original_tag = TUR
		}

		available = {
			NOT = {
				has_country_flag = Paying_debt
			}
			has_war_with = ENG
			has_war_with = FRA
			check_variable = { var = GRE_debt_to_the_ifc_britain_var value = 60 compare = equals }
		}

		cost = 120

		fire_only_once = yes

		ai_will_do = {
			factor = 1
			modifier = {
				factor = 0
				is_historical_focus_on = yes
			}
			modifier = {
				factor = 100
				has_war_with = ENG
			    has_war_with = FRA
			}
			modifier = {
				factor = 30
				
			}
		}

		visible = {
			OR = {
				has_idea = Ottoman_debt_administration
				has_idea = Ottoman_debt_administration_1
				has_idea = Ottoman_debt_administration_2
			}
			has_war_with = ENG
			has_war_with = FRA
		}

		complete_effect = {
		    custom_effect_tooltip = Debt_must_be_falf_paid
			if = {
			    limit = {
				    has_idea = Ottoman_debt_administration
				}
				remove_ideas = Ottoman_debt_administration
				else_if = {
				    remove_ideas = Ottoman_debt_administration_1
				}
				else_if = {
				    remove_ideas = Ottoman_debt_administration_2
				}
			}
		}
	}	
}